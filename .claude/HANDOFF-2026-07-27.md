# Хэндофф — закрытие сессии 2026-07-26 (в новую сессию)

`main` = `279793a`, синхрон с origin, всё смержено, открытых PR нет, прод 200/200/200.
Читать вместе с `.claude/specs/2026-07-26-r4-cutover-SPEC.md` и `.claude/reports/2026-07-26-r4-cutover.md`.

## Где мы — Фаза 2 (доставка) ЗАКРЫТА, демо на воспроизводимом стеке

| Кусок | Статус |
|---|---|
| R-1…R-3 воспроизводимый compose (#31→#34) | **в проде** |
| Staking-дефолт кода → V3 + guard-тест (#35) | **в проде** |
| webapp «Получить демо-баланс» (#36) | **в проде** |
| **R-4 cutover** на `/opt/oltinpay` (проект `oltinpay-app`) | **ИСПОЛНЕН + задеплоен** |
| webapp фиксы: онбординг-луп (#38), retry-экран storage-error (#39) | **в проде** |

## Живое состояние (7demo, проверено чтением 2026-07-26)
- Демо держит проект **`oltinpay-app`** из `/opt/oltinpay` (git-driven, `main`). Старый
  `oltinchain` api/webapp сняты. Три урла 200; `oltinpay_db` (та же БД, данные целы).
- `KEY_BANK_OPS` = **минтер-only** bank-ops EOA `0x6d2c91B09bD6b97A46F364951ba4451F1bA551BE`
  (MINTER=true, ADMIN=false; grant tx `0xc66759c3…e4bc5`). Админ/деплоер-ключ на сервере
  НЕ лежит — только локально `contracts/.env` (там же `BANK_OPS_PRIVATE_KEY`). INDEXER=true.
- Откатные образы (`oltinchain-oltinpay-*:latest`, `*:pre-cutover-2026-07-22`) целы —
  **`docker image prune -a` запрещён до R-5.**

## Точка возобновления
1. **Клик-приёмка демо-баланса — за Капитаном** (нельзя headless): Mini App → онбординг →
   кошелёк → «Получить демо-баланс» → должно прийти **1000 UZD**. Это финальная живая
   проверка сессии; плумбинг весь на месте.
2. **R-5** (ретайр `oltinchain`, дроп `oltinchain_db` — К-5): отдельный ops-шаг. До него
   откатные образы не пруним.
3. **Трек «чистка сервера»** — Ревьюер снял read-only инвентарь; план по этапам в
   `.claude/specs/2026-07-27-server-cleanup-PLAN.md`. Гейт перед стартом — владение
   продуктами (Капитан) + тайминг. Уровень-3 карантин (общая инфра, oltinchain-образы,
   `internal/proxy`, caddy) — НЕ ТРОГАТЬ.

## Оперативные напоминания
- Фиды/киперы протухают без cron — перед показом `npm run keeper:all` из `contracts/`
  (`nvm use 20`); временный крон отложен (лимиты). MINTER на UZD2: деплоер + новый bank-ops.
- Деплой webapp/api: на сервере `git reset --hard origin/main` + `docker compose build/up -d`
  нужного сервиса, всегда проект `oltinpay-app` (имя `oltinpay` занято лендингом).
- Правила: без AI-подписей; один PR — одна причина; мержим по зелёному CI; связь
  Инженер↔Ревьюер через Капитана; ключи/значения секретов в транскрипт не печатаем.

## Хвост-фолоу-апы (не блокируют)
- Retry-экран (#39) при устойчивой ошибке storage не роутит на онбординг (защита от
  перезаписи кошелька) — богаче было бы авто-ретрай с бэкоффом, опциональная доработка.
- `caddy:2-alpine` — расхождение «4 контейнера числится / 1 виден» (из инвентаря) —
  разобрать чтением до любой чистки.
