# Хэндофф — передача в новую сессию (вечер 25 июля)

Написан Инженером при закрытии сессии (контекст исчерпан). Читать первым,
вместе с `.claude/PLAN-2026-07-23.md` (ред. 2) и `.claude/BASELINE-2026-07-22.md`.
До дедлайна конкурса (10 августа) — **16 дней**.

## Где мы — Фаза 2 идёт, R-1…R-3 смержено

`main` = `34095ae`, CI зелёный, открытых PR нет, дерево чистое
(шум `artifacts-zk`/`cache-zk` — известный, не трогаем).

| Пункт | Статус |
|---|---|
| **Фаза 1 «Обеспечение»** | **ЗАКРЫТА** (P1-A #15, P1-E #16, P1-B, V3.1 #24/#25/#27, P1-C, P1-D #28) |
| R-1…R-3 воспроизводимый compose | **смержено #31**, отчёт #32 — **ждёт вердикт Ревьюера (Гейт-2)** |
| R-4 переключение на `/opt/oltinpay` | следующий трек (спека + ops) |
| R-5…R-8, P4-A, P5-A | открыты |

## Точка возобновления — РОВНО здесь

1. **Прочитать вердикт Ревьюера по R-1…R-3** (Гейт-2 висит). Открытый вопрос из
   отчёта: `mem_limit: 512m` в новом compose против продовых 150M — оставить
   или сровнять до R-4.
2. **Спека R-4** на Гейт-1: погасить `oltinpay-api`/`oltinpay-webapp` проекта
   `oltinchain`, поднять из `/opt/oltinpay` после `git reset --hard origin/main`,
   alembic, проверить три урла. Обязательные элементы: **полный прогон отката
   сразу после cutover** (down -p oltinpay-app → up из /opt/oltinchain → 200 →
   накатиться обратно — правило Ф0-3, уже отрепетировано); независимая проверка
   чтением (коды урлов, метки compose-проекта, DATABASE_URL внутри контейнера);
   `.env` на сервере по новому `.env.example` (ключи — с живого контейнера);
   К-4 (`app.oltinchain.com` — ретайрим или алиас) нужен к R-4.
3. **Понедельник 27.07 — решение `MAX_SOURCE_AGE`** по логу наблюдения
   (`/root/chainlink-xau-updatedAt.log` на 7demo; ожидание ≤ 23 ч → кандидат
   72ч → 26ч). Туда же «PR дефолтов»: `MAX_TOKEN_AGE` 600, `MAX_JUMP_BPS` 5%.

## Факты, которые НЕ перечитывать заново (проверены)

- **Приёмка Ф1:** buy/sell ✓, `totalSupply` 2519.43 г (покрытие 198.45%),
  gasless tx `0xb86555c4383d950aff587aa551c6f8f8e4c91a386c32f3c6db0b1e86423d93c1`.
- **V3.1:** UZD `0x51232fd0065bD2ca50551761Acef476E3CDf02aA`, Exchange
  `0x99D733E64eb60c3B3D5f3DeDe4CC4adC92BCd1c9`; старые — retired.
- **R-1…R-3:** compose проекта = `oltinpay-app`; свои postgres/redis убраны
  (shared-инфра на `internal`, db `oltinpay_db`, redis db 3); API собирается
  по `uv.lock` (в репо, `uv lock --check` в CI); бот за profile `bot`
  (выключен; **статус живого бота для демо — вопрос Капитану, открыт**).
- **Постер киперов** `0xfaFB46cC23705058EE9E1a96f64B0f273B87405e` — ключ
  ТОЛЬКО на 7demo (`/root/oltinpay-keeper/poster.key`, 600). Не регенерировать.

## ⚠️ Оперативные напоминания

- **Фиды протухают без cron**: перед работой/показом — `npm run keeper:all`
  из `contracts/` (env укомплектован). Cron киперов = P4-A (открыт).
- **Временный крон киперов на 7demo — ОТЛОЖЕН решением Капитана 25.07**:
  лимиты исчерпаны, обновятся ~29.07. Задача на возобновление: развернуть
  киперы на 7demo (`/root/oltinpay-keeper/`: клон contracts из main + npm ci +
  .env с постер-ключом) + cron `*/10 * * * *` на `keeper:all` + проверка
  `updatedAt` через 20 мин. До тех пор — ручной прогон каждое утро.
- **Node 20** для contracts (`nvm use 20`), zksolc ломается на 24.
- Ловушки сервера (BASELINE §5): живое демо = проект `oltinchain`; `oltinpay`
  занят лендингом; деплой только `-p oltinpay-app`; `docker image prune -a`
  на 7demo запрещён до R-5 (откат зависит от локальных образов).
- На 7demo НЕ оставлять мусор: временные клоны/образы убирать (убрано 25.07).
- Локального докера на этой машине нет — compose/build проверки гонять на 7demo
  через /tmp-клон ветки.

## Правила (кратко, полные — в прошлых хэндоффах/PLAN)

Без AI-подписей; один PR — одна причина; мержим по зелёному CI; красный тест →
зелёный для кода; чтение состояния до/после для ops; полные хеши в отчётах;
связь Инженер↔Ревьюер — только через Капитана; один трек за раз.
