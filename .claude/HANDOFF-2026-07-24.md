# Хэндофф — передача в новую сессию (вечер 24 июля)

Написан Инженером при закрытии сессии. Читать первым, вместе с
`.claude/PLAN-2026-07-23.md` (ред. 2) и `.claude/BASELINE-2026-07-22.md`.
До дедлайна конкурса (10 августа) — **17 дней**.

## Где мы — ФАЗА 1 «ОБЕСПЕЧЕНИЕ» ЗАКРЫТА

`main` = `7b7c89c`, CI зелёный, открытых PR нет, дерево чистое
(шум `artifacts-zk`/`cache-zk`/`uv.lock` — известный, не трогаем).

| Пункт | Статус |
|---|---|
| P1-A киперы (heartbeat, UZS, резерв) | смержено #15 |
| P1-E цена 24/7 (медиана PAXG+XAUT, Chainlink-детектор) | смержено #16 |
| P1-B постер-ключ + роли + 0.03 ETH | сделано 24.07 (ниже) |
| V3.1 перевыпуск UZD+Exchange | смержено #24, #25 |
| P1-C сид демо-банка | выполнен из main (#27 — follow-ups) |
| P1-D gasless-смоук | **GREEN на проде** #28, отчёт #29 |

**Приёмка Ф1:** покупка/продажа ✓, `totalSupply` = **2519.43 г** (резерв 5000 г,
покрытие **198.45%**), gasless tx
`0xb86555c4383d950aff587aa551c6f8f8e4c91a386c32f3c6db0b1e86423d93c1`
(клиент −0 ETH на газ, fee 0.000189 г OLTIN, майнинг ~1 с).
Критерий «дашборд покрытия» → **P2-C** (решение Капитана 24.07, записано в PLAN).

## Ключевые адреса (V3.1, актуальные — docs/DEPLOYMENTS.md)

- UZD **`0x51232fd0065bD2ca50551761Acef476E3CDf02aA`** (деплоер = admin/minter)
- Exchange **`0x99D733E64eb60c3B3D5f3DeDe4CC4adC92BCd1c9`** (OLTIN minter,
  спящий минтер старого Exchange отозван)
- OLTIN `0x906bcf6c…`, фиды XAU/UZS/Reserve — без изменений
- Старые UZD `0x95b30Be4…` / Exchange `0xc367D776…` — **retired** (не
  использовать нигде)
- Постер киперов `0xfaFB46cC23705058EE9E1a96f64B0f273B87405e` — ключ ТОЛЬКО на
  7demo (`/root/oltinpay-keeper/poster.key`, 600). **Не регенерировать.**
- Демо-клиенты ×3 — адреса в DEPLOYMENTS.md, ключи в локальном `contracts/.env`

## ⚠️ Фиды НЕ самоподдерживаются (как и вчера)

Cron киперов не настроен (это P4-A). XAU/Reserve протухают за час, UZS — за
3 суток. **Перед работой/показом — `npm run keeper:all`** из `contracts/`
(env уже укомплектован: Alchemy, аттесторы, KEY_* = деплоер).

## Следующий трек — Фаза 2 «Воспроизводимая доставка» (★)

Спека **R-1…R-3** на Гейт-1. Обязательные ответы спеки (бриф Ревьюера):
(а) **пиннинг зависимостей API** — сейчас образ строится `pip install --no-cache-dir .`
без локфайла, R-4 пересобирает: новая транзитивная зависимость ломает сборку
ровно в момент переключения; (б) **`oltinpay-bot`** — есть в репозиторном
compose, контейнера на сервере нет: профиль, scale 0 или осознанный запуск;
(в) **`.env.example` по фактическим ключам живого контейнера**, не из головы.
Плюс правило из Ф0-3: **R-4 не выполнен без полного прогона отката сразу после
cutover** (откатились → 200 → накатились обратно). Ловушки сервера — BASELINE §5
(проект `oltinchain` держит живое демо; `oltinpay` занят лендингом; деплой
только с `-p oltinpay-app`; `docker image prune -a` на 7demo запрещён до R-5).

## Хвосты (не блокируют)

- **«PR дефолтов»**: `MAX_TOKEN_AGE` 3600→600, `MAX_SOURCE_AGE` 72ч→26ч,
  `MAX_JUMP_BPS` 10%→5%. По `MAX_SOURCE_AGE` — решение **в понедельник 27.07**
  по данным наблюдения (cron `chainlink-xau-observe.sh` на 7demo, лог
  `/root/chainlink-xau-updatedAt.log`; ожидание: максимум разрыва ≤ 23 ч).
- **P4-A**: контракт в PLAN (cron только `keeper:all`/10 мин, last-success
  дедлайн ~30 мин, дебаунс алертов на серию refuse, RBF, алерт≠автодолив).
  Там же — end-to-end пост постер-ключом (код на сервере ещё не развёрнут).
- **P5-A** — верификация исходников на эксплорере (флаки zksolc-verify).
- V2-стейкинг `0x63e537…` в `api/config.py` — зафиксировано для P3, не трогать.
- Курс паймастера истекает **2026-08-21** (после дедлайна, но напомнить).

## Что нужно машине (секреты переносить руками, репо публичный)

| Что | Зачем |
|---|---|
| `contracts/.env` (PRIVATE_KEY, MAINNET_RPC_URL + ALCHEMY_PRICES_URL с ключом Alchemy, KEY_* = деплоер, адреса V3.1, DEMO_KEY_1..3) | киперы, сид, смоук |
| `root@7demo` по ключу | сервер, крон наблюдения, будущий cron киперов |
| **Node 20** (`nvm use 20`) | zksolc ломается на Node 24 |
| `gh` на `temrjan/OltinPay` | PR/CI |

## Правила (из кода не выводятся)

- Коммиты/PR **без AI-подписей**; один PR — одна причина; мержим по зелёному CI.
- Роли: Капитан решает, Инженер один трек за раз, Ревьюер гейтит. Связь
  Инженер↔Ревьюер — только через Капитана.
- Гейты: 1 (спека) + 2 (диф) для кода, один для ops. Доказательства: для кода —
  красный тест → зелёный; для ops — чтение состояния до/после, не вывод скрипта.
- Полные хеши транзакций в отчётах и доках, не усечённые.
