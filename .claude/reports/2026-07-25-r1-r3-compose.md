# Отчёт: R-1…R-3 — воспроизводимый compose (PR #31)

Дата: 2026-07-25 · Инженер · Гейт-2 (диф)
Спека: `.claude/specs/2026-07-25-r1-r3-compose-SPEC.md` (go 25.07)

## Сделано — по пунктам спеки

1. **`docker-compose.yml`:** `name: oltinpay-app`; свои postgres/redis и их
   volumes убраны; `internal`/`proxy` — external; `oltinpay-api` на
   `env_file: .env` (DATABASE_URL/REDIS_URL не конструируются); healthcheck api;
   `oltinpay-bot` за `profiles: ["bot"]`.
2. **Пиннинг (а):** `uv.lock` закоммичен (78 пакетов); Dockerfile — `uv sync
   --locked --no-dev` (uv 0.11.26 запинен, два слоя: deps → project);
   `api.yml` += шаг `uv lock --check` (прошёл в CI этого же PR).
3. **Бот (б):** profile, по умолчанию выключен. Статус живого бота — ушёл
   Капитану (не блокирует).
4. **`.env.example` (в):** ровно ключи живого контейнера + `ADMIN_PRIVATE_KEY=`
   пустая (К-6). Старая версия (PG_PASSWORD/REDIS_PASSWORD, ремап
   OLTINPAY_BOT_TOKEN, ссылка на потерянный ключ `0xa0A7…`) переписана.
5. **DEPLOY.md:** абзац о способе подъёма — отложен (см. «Не сделано»).

## Как тестировал (на 7demo — локально докера нет)

```
docker compose config:  ✓ name oltinpay-app  ✓ нет postgres/redis
  ✓ internal/proxy external  ✓ бота нет без профиля  ✓ есть с --profile
  services: oltinpay-api, oltinpay-webapp
docker build (api, по локфайлу): зелёный
  fastapi 0.139.2 == lock 0.139.2; httpx 0.28.1 == lock 0.28.1;
  импорты fastapi/sqlalchemy/httpx/pydantic/uvicorn/jose/eth_account/
  alembic/asyncpg/redis — все работают в образе
CI: api test pass (включая новый lock-check), остальные воркфлоу не триггернулись
Временный клон /tmp/r1r3-check и check-образ на сервере убраны.
```

## Не сделано / отложено

- Абзац в `docs/DEPLOY.md` — в спеке был пунктом 6; переношу в R-6 (там
  DEPLOY.md переписывается целиком вместе с deploy.yml — абзац сейчас
  устарел бы до его применения). Если Ревьюер настаивает на промежуточной
  правке — добавлю отдельным мини-PR.
- Подъём стека на сервере — R-4 (здесь: config + сборка, как и обещала спека).

## Замечено, не трогаю

- Корневой `.env.example` раньше ссылался на потерянный ключ `0xa0A7…` как на
  «admin wallet» — переписано в этом PR; историческая правка прошла неявно в
  контексте К-6 (пустая строка до решения).

## Вопросы Ревьюеру

1. `oltinpay-api` в репозиторном compose имеет `mem_limit: 512m` (у живого —
   150M в `/opt/oltinchain`). Новый API тяжелее (PR-2), но 512 — с запасом
   против 150 живого. Оставить 512 или сровнять с продовым 150M до R-4?
